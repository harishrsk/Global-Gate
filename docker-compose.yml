services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: globalgate
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: globalgate_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
